pcmt_product_drafts_index:
    path: /drafts/
pcmt_product_drafts_api:
    path: /rest/drafts/
    defaults: { _controller: pcmt_product.controller.rest.drafts:getList }
    methods: [GET]
pcmt_product_drafts_params:
    path: /rest/drafts/params/
    defaults: { _controller: pcmt_product.controller.rest.drafts:getListParams }
    methods: [GET]
pcmt_product_drafts_delete:
    path: /rest/draft/{id}/
    defaults: { _controller: pcmt_product.controller.rest.drafts:rejectDraft }
    requirements:
        id: \d+
    methods: [DELETE]
pcmt_product_drafts_approve:
    path: /rest/draft/approval/{id}/
    defaults: { _controller: pcmt_product.controller.rest.drafts:approveDraft }
    requirements:
        id: \d+
    methods: [PUT]
