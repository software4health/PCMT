image: docker:stable

stages:
  - build
  - deploy

services:
  - docker:dind

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_DRIVER: overlay2

before_script:
  - docker info

build:
  stage: build
  script:
    - cd docker
    - pwd
    - ls -al
    - sh -c $(pwd)/build-images.sh
  #  - docker build -f docker/pim/Dockerfile -t pcmt/pim --build-arg AKENEO_VER="v3.1.10" docker/pim/

deploy:
  stage: deploy
  script:
    - echo "...deploy to env here..."