image: docker:stable

stages:
  - build
  - deploy

services:
  - docker:dind

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_DRIVER: overlay2

build:
  stage: build
  script:
    ./docker/build-images.sh

deploy:
  stage: deploy
  script:
    - echo "...deploy to env here..."